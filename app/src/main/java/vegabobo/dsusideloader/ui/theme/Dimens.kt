package vegabobo.dsusideloader.ui.theme

import androidx.compose.ui.unit.dp

/** Horizontal inset shared by every scrollable screen, so cards line up across destinations. */
val ScreenHorizontalPadding = 16.dp

/** Vertical gap between the cards of a screen. */
val ScreenItemSpacing = 12.dp

/** Vertical gap between grouped rows inside a list container. */
val GroupedItemSpacing = 2.dp
